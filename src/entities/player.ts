export class Player {
    name: string;
    letter: string;
    color: string;

    constructor(name: string, letter: string, color: string) {
        this.name = name;
        this.letter = letter;
        this.color = color;
    }
}
